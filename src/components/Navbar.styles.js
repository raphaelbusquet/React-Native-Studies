import { StyleSheet } from "react-native";

export default StyleSheet.create({
    app: {
        backgroundColor: '#ccc',
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        marginBottom: '10%',
        padding: '6%',
        borderRadius: '10%',
    },

    title: {
        fontSize: 30,
        fontWeight: 'bold',
    }
})